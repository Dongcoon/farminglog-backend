<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.farmlog.organizationdashboard.mapper.OrganizationAccessMapper">
  <!-- 역할 승계 없이 현재 ACTIVE 조직의 ACTIVE ORG_ADMIN 행만 exact 확인한다. -->
  <select id="findActiveOrgAdmin" resultType="com.farmlog.organizationdashboard.entity.OrganizationAccessRow">
    SELECT o.id,o.name,o.org_type,o.status
    FROM organization o
    JOIN organization_member om ON om.organization_id=o.id
      AND om.user_id=#{userId} AND om.role='ORG_ADMIN' AND om.status='ACTIVE'
    WHERE o.id=#{organizationId} AND o.status='ACTIVE' AND o.deleted_at IS NULL
  </select>
</mapper>
