<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.farmlog.common.tenant.FarmMembershipMapper">

    <select id="findActiveMemberRole" resultType="string">
        SELECT fm.role
        FROM farm_member fm
        JOIN farm f ON f.id = fm.farm_id
        WHERE fm.farm_id = #{farmId}
          AND fm.user_id = #{userId}
          AND fm.status = 'ACTIVE'
          AND f.deleted_at IS NULL
        LIMIT 1
    </select>

    <select id="findActiveMemberRoleForUpdate" resultType="string">
        SELECT fm.role
        FROM farm_member fm
        WHERE fm.farm_id = #{farmId}
          AND fm.user_id = #{userId}
          AND fm.status = 'ACTIVE'
        LIMIT 1
        FOR UPDATE
    </select>

</mapper>
